# KO 문법 이유: PEP 517 빌드 도구가 [build-system] 표를 읽어 setup.py 실행에
# 필요한 패키지를 먼저 준비합니다.
# KO 논리 이유: torch CUDA extension은 setuptools/wheel/torch/ninja가 있어야
# Windows에서도 같은 방식으로 증분 빌드됩니다.
[build-system]
requires = ["setuptools", "wheel", "torch", "ninja"]
build-backend = "setuptools.build_meta"
